name: Compile Crux Kernel

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Kernel Source
        uses: actions/checkout@v4
        with:
          ref: test

      - name: Install Build Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y bc build-essential libncurses5-dev libssl-dev texinfo \
            libgmp3-dev libmpfr-dev libmpc-dev libcap-dev zlib1g-dev python3 \
            bison flex ccache libelf-dev lz4 liblz4-tool libstdc++6 openjdk-11-jdk zip unzip curl git

      - name: Fetch Toolchains (ZyC-Clang 2026 & AOSP GCC)
        run: |
          mkdir -p $GITHUB_WORKSPACE/toolchain
          cd $GITHUB_WORKSPACE/toolchain
          
          echo "Downloading latest ZyC-Clang 16..."
          curl -LO https://github.com/ZyCromerZ/Clang/releases/download/16.0.6-20260510-release/Clang-16.0.6-20260510.tar.gz
          mkdir -p zyc-clang
          tar -xzf Clang-16.0.6-20260510.tar.gz -C zyc-clang
          
          echo "Downloading GCC 64-bit..."
          curl -LO https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9/+archive/refs/tags/android-11.0.0_r47.tar.gz
          mkdir -p gcc64
          tar -xzf android-11.0.0_r47.tar.gz -C gcc64
          
          echo "Downloading GCC 32-bit..."
          curl -LO https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/arm/arm-linux-androideabi-4.9/+archive/refs/tags/android-11.0.0_r47.tar.gz
          mkdir -p gcc32
          tar -xzf android-11.0.0_r47.tar.gz -C gcc32

      - name: Start Kernel Compilation
        run: |
          cd $GITHUB_WORKSPACE
          
          # 精准锁定新版 ZyC-Clang 的实际 bin 路径
          if [ -d "$GITHUB_WORKSPACE/toolchain/zyc-clang/bin" ]; then
            REAL_CLANG_PATH="$GITHUB_WORKSPACE/toolchain/zyc-clang/bin"
          else
            REAL_CLANG_PATH="$GITHUB_WORKSPACE/toolchain/zyc-clang"
          fi
          
          export GCC64_PATH=$GITHUB_WORKSPACE/toolchain/gcc64/bin
          export GCC32_PATH=$GITHUB_WORKSPACE/toolchain/gcc32/bin
          
          # 【降维打击】建立最高优先级的环境拦截
          # 这样不管是内核调用 clang, gcc 还是 ld.lld，都会被立刻导流到最新版 ZyC-Clang 中
          mkdir -p $GITHUB_WORKSPACE/bin_override
          ln -sf $REAL_CLANG_PATH/clang $GITHUB_WORKSPACE/bin_override/clang
          ln -sf $REAL_CLANG_PATH/clang $GITHUB_WORKSPACE/bin_override/aarch64-linux-gnu-clang
          ln -sf $REAL_CLANG_PATH/ld.lld $GITHUB_WORKSPACE/bin_override/ld.lld
          ln -sf $REAL_CLANG_PATH/ld.lld $GITHUB_WORKSPACE/bin_override/ld
          ln -sf $REAL_CLANG_PATH/llvm-ar $GITHUB_WORKSPACE/bin_override/llvm-ar
          ln -sf $REAL_CLANG_PATH/llvm-nm $GITHUB_WORKSPACE/bin_override/llvm-nm
          ln -sf $REAL_CLANG_PATH/llvm-objcopy $GITHUB_WORKSPACE/bin_override/llvm-objcopy
          ln -sf $REAL_CLANG_PATH/llvm-objdump $GITHUB_WORKSPACE/bin_override/llvm-objdump
          ln -sf $REAL_CLANG_PATH/llvm-strip $GITHUB_WORKSPACE/bin_override/llvm-strip
          
          # 把我们强行包装好的覆盖目录放到 PATH 的最顶头
          export PATH=$GITHUB_WORKSPACE/bin_override:$REAL_CLANG_PATH:$GCC64_PATH:$GCC32_PATH:$PATH
          
          export ARCH=arm64
          export SUBARCH=arm64
          
          # 1. 生成配置文件
          make O=out ARCH=arm64 crux_defconfig
          
          # 2. 正式编译（不传大堆绝对路径了，全靠上面的最高优先级 PATH 强行拦截注入）
          echo "Starting make process with forced path overriding..."
          make -j$(nproc --all) O=out \
                      ARCH=arm64 \
                      CC=clang \
                      CLANG_TRIPLE=aarch64-linux-gnu- \
                      CROSS_COMPILE=aarch64-linux-android- \
                      CROSS_COMPILE_ARM32=arm-linux-androideabi-

      - name: Verify and Package Output
        run: |
          cd $GITHUB_WORKSPACE
          if [ -f "out/arch/arm64/boot/Image.gz-dtb" ]; then
            echo "Kernel compiled successfully!"
            mkdir -p output
            cp out/arch/arm64/boot/Image.gz-dtb output/
            find out/ -name "*.ko" -exec cp {} output/ \; 2>/dev/null || true
            cd output && zip -r ../crux-kernel-clean.zip *
          else
            echo "ERROR: Kernel image not found. Compilation might have failed."
            exit 1
          fi

      - name: Upload Kernel Artifact
        uses: actions/upload-artifact@v4
        with:
          name: crux-kernel-clean
          path: crux-kernel-clean.zip
